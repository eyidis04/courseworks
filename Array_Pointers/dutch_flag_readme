You have an array which has include 0, 1, 2values and you must sort this array with an algorithm with O(n) complexity.

I used a different algorithm than can be found on the internet. It's little bit hard to read and semms little bit more unnecessary but it's mine :D
